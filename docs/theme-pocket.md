# 主题二 · 口袋启动器

2026-09-14：按用户选定的第 2 张蓝白样稿实现，不再重新改变方向。移动端保留三列六图标、四个分类、文本整理卡、追番入口及首页／游戏库／工具三项导航。桌面端自然展开为六列。

## 代码边界

- 标识为 `theme-pocket`。2026-09-16 起作为默认主题：首次访问、无效偏好和退役的 `theme-one`／`theme-two` 回退到本主题，已保存的有效主题选择保持不变。SSR、首屏初始化及 PWA 启动配色统一读取此默认值；退役主题不重新启用。
- `features/themes/theme-pocket/`：独立首页、导航、页头、分类模型、文案及专注训练视图。
- `styles/themes/theme-pocket/`：独立 tokens、首页、通用表面、工具、游戏，以及按路由加载的经典游戏／专注训练样式。
- 仅复用中性目录、页头接口、图标族与业务控制器；不导入主题一、三、四的视觉组件或样式。没有新增图标库。
- 现有 16 个游戏和 6 个工具入口全部保留。益智分类含专注训练，街机分类含 Bingo；不增加设置中心导航。
- 不修改游戏算法、追番记录、跨设备传输、语言初始化、3D 引擎或 Redis 协议。

## 插画来源与提示词

所选样稿：`exec-27e5a9fb-12e8-4b4f-b07e-bf340342585d.png`，853 × 1844，归一到 390 × 844 对照。

使用内置 ImageGen 逐张生成，原图仅作为风格与构图参考，没有将整页截图作为页面背景。首批 7 个生产文件位于 `public/images/theme-pocket/`，均为 320 × 320、WebP quality 85，总计 82,548 字节；文档图保留透明通道。原 PNG 保留在被忽略的 `output/theme-pocket-assets/`。

| 文件 | 提示词核心 | 字节 |
| --- | --- | ---: |
| `bingo.webp` | 参考左上游戏图；立体红色 7、蓝色 3、黄色 5 宾果球，蓝色金属球笼背景；方形满幅，无 UI 或外部圆角蒙版 | 17992 |
| `tiles.webp` | 参考上中游戏图；微倾斜亮黄色立体方块，白色准确文字 2048，奶油色浮雕方格背景；无 UI | 8252 |
| `snake.webp` | 参考右上游戏图；红橙色圆润大眼蛇、青绿色砖面、左下黄色食物球；立体光泽，无文字 | 8898 |
| `gomoku.webp` | 参考左下游戏图；木质棋盘与八枚黑白棋子，保留参考排列，写实立体满幅 | 15554 |
| `chess.webp` | 参考下中游戏图；象牙色马棋子近景，深蓝／棕色背景与失焦棋子；无文字 | 10890 |
| `tetris.webp` | 参考右下游戏图；蓝紫黄红橙绿立体俄罗斯方块，深蓝棋格背景，保留堆叠方向 | 7890 |
| `documents.webp` | 参考工具卡；分层白蓝纸张、四条蓝色横线、右下蓝色圆形白勾徽章，透明背景，无卡片或 UI | 13072 |

## 离线与验证

- 新插画目录加入完整离线资源生成器及目录一致性测试，SW 版本升到 `v22`。沿用现有确认更新机制，不强制激活、不清空业务数据。
- `pnpm verify`：Lint、TypeScript、两套 3D 构建和 Next.js 构建通过；78 个测试文件通过、1 个跳过，568 项测试通过、2 项跳过。
- 22 个二级页面均在 390px 与 320px 完成浏览器布局检查，无横向溢出；首页另检查 1280px 桌面、中文／英文／泰文、分类与导航、刷新后主题保留。
- 实操文本整理，测试输入去重后得到 `Pocket QA\n\nReady`；专注训练开始后点击 1，下一目标为 2、进度为 1/25，未完成或停止测试以写入成绩。
- 主题一、三、四文本工具代表页回归，既有布局继续独立生效。
- 对照图和页面证据：`output/theme-pocket-qa/`；设计审查详见根目录 `design-qa.md`。
- 未执行 iOS／Android 真机 PWA 安装或断网冷启动，不能以构建与离线清单测试替代真机验收。既有 3D 大 chunk 与纸张纹理运行时路径提示仍存在。
- 当前是本地实现与预览，未提交、未推送、未部署。

## 2026-09-14 后续：补齐所有游戏封面

- 根据用户截图反馈，补齐原先使用线框占位的 10 个游戏：Bingo 卡片、中国象棋、围棋、黑白棋、舒尔特方格、数字字母交替追踪、扫雷、数独、记忆翻牌、霓虹打砖块。
- 使用内置 ImageGen，以首批 `tiles.webp` 和 `gomoku.webp` 作为材质／光照参考逐张生成独立封面；完整提示词及生产路径见 `docs/theme-pocket-cover-prompts.md`。
- 新文件仍位于 `public/images/theme-pocket/`，统一 320 × 320、WebP quality 85。新增 163,872 字节；全套 16 张游戏封面与 1 张工具插画总计 246,420 字节。首屏精选素材仍为 82,548 字节，没有替换原有 7 张插画或调整布局。
- `POCKET_ART` 覆盖全部 16 个游戏；新增“目录必须逐项有独立封面”约束，先确认旧映射缺少 10 项而失败，再完成映射与素材后通过。保留单张 35 KB、全套 250 KB、精选 100 KB 的预算检查。
- 离线清单自动包含新增文件，当前总计 287 项。此轮与前一轮主题二均尚未提交发布，沿用同一待发布 SW 版本 v22。
- 390px 棋类／益智／街机分类全部图片加载成功，无线框占位；完整游戏库 16/16 图片加载成功。320px 益智分类图像等宽等高、无横向溢出；点击记忆翻牌封面正确进入原游戏页。
- 最终 `pnpm verify` 通过：78 个测试文件、569 项测试通过，1 文件／2 项测试仍跳过。既有 3D 构建警告未变；未做真机 PWA 断网冷启动。
- 新一轮视觉证据保存在 `output/theme-pocket-covers-qa/`。没有修改游戏规则、数据或其他主题；仍未提交／推送。

## 2026-09-14 后续：统一工具入口插画

- 工具列表的 6 个入口全部使用同系列立体插画：首页追番入口同步复用电视插画，文本整理复用已有透明文档图。顶部／底部导航和跳转箭头仍是轻量操作图标，不修改工具业务或其他主题。
- 内置 ImageGen 以已有文档图和 2048 封面为材质／光影参考，新生成追番、文本加解密、二维码、JSON、Base64 五张图片。完整提示词见 `docs/theme-pocket-tool-prompts.md`；二维码封面仅为装饰插画，不包含可用传输数据。
- 新文件统一 320 × 320 WebP quality 85，合计 52,102 字节；6 张工具图合计 65,174 字节，全套 22 张主题插画为 298,522 字节。首页精选加两张工具图共 91,716 字节。原游戏素材预算继续独立校验，工具全套新增 100 KB 上限。
- `POCKET_TOOL_ART` 独立于中性目录和游戏映射。回归检查先确认首页未使用工具映射、五张资源缺失时失败，接入后通过；离线清单自动包含新文件，总计 292 项，沿用当前待发布 SW v22。
- 浏览器检查 390 × 844、320 × 740 和 1280 × 900：6/6 图片加载成功、均为 48px 方形，无横向溢出；桌面保持双列。首页两个工具图正常，追番卡片底部位于导航顶部上方约 10px。点击 JSON 工具入口进入原二级页面。
- `pnpm verify` 通过：Lint、TypeScript、两套 3D 构建、Next.js 构建、78 个测试文件／570 项测试；1 文件／2 项仍跳过。既有 3D 警告不变；未进行真机 PWA 离线冷启动。
- 视觉证据保存在 `output/theme-pocket-tools-qa/`。未提交、未推送、未部署。

## 提交授权

以上“未提交／未推送”为各轮视觉验收时的快照。2026-09-14 用户已授权提交并同步完整主题二及后续游戏／工具插画；最终提交与远端同步结果以 Git 记录为准。本次不额外执行部署，生成原图及浏览器截图继续保留在本地忽略目录。
